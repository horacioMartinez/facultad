#include "Peso.h"
#include <string>
#include <vector>
#include <stack>
void Peso::operar(int &puntero_datos, std::vector<signed char> &datos, int &pos,
                  std::stack<int> &pilaPosLoops, int &ejecutar) {
  if (ejecutar == 1)
    ejecutar--;
}
const char* Peso::getCodigo() {
  return "return 0;";
}
